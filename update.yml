---
- name: Update Linux host
  hosts: all
  

  tasks:
    - name: Get all updates
      ansible.builtin.apt:
        update_cache: true
        autoremove: true
        autoclean: true
        become: yes
